use std::collections::{hash_map, HashMap};
fn main() {
    println!("Hello, world!");
    let a = HashMap
}
vim.lsp.inlay_hint.is_enabled(bufnr:number) is deprecated, use vim.lsp.inlay_hint.is_enabled(filter:table) instead. :help deprecated
